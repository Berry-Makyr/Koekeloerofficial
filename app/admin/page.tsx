import AdminDashboard from '@/components/admin/AdminDashboard';

export const metadata = {
  title: 'Catalog Manager & Listing Admin | Koekeloer Gansbaai',
  description: 'Manage and update Koekeloer product listings, photo galleries, stock status, and pricing.',
};

export default function AdminPage() {
  return <AdminDashboard />;
}
